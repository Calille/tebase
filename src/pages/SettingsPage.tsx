import React from "react";
import PageLayout from "@/components/tebase/PageLayout";
import Settings from "@/components/tebase/settings/Settings";

const SettingsPage = () => {
  return (
    <PageLayout title="Settings">
      <Settings />
    </PageLayout>
  );
};

export default SettingsPage;
